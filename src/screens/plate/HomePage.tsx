import React from 'react';
import { View, Text } from 'react-native';

const HomePage = () => {
    return (
        <View>
            <Text>Homepage(plate list) Screen</Text>
        </View>
    );
}
export default HomePage;